# Pinned for reproducible unattended-server deploys.
# To bump: change the pins, reinstall, and re-run the DEMO.md walkthrough.
mcp>=1.2.0
caldav==3.2.1
icalendar==7.2.2
# starlette + uvicorn ship as dependencies of `mcp` and power the HTTP transport.
